

<%= form_for [ @user, @post, @comment ] do |f| %>
 
  <p>
    <%= f.label :body, "Your Comment" %><br/>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit 'Submit' , class: 'btn btn-primary' %>
  </p>
<% end %>