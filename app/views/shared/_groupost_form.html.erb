<%= form_for [@group, @groupost] do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Post in group..." %>
  </div>
  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>