

<span class="glyphicon glyphicon-eye-open" aria-hidden="true" size: "20">

	<small><strong><%= time_ago_in_words(activity.created_at) %> ago</strong></small>

   <% if activity.owner.picture? %><%= image_tag activity.owner.picture.url, :size => "30x30"  %>
       
        <%end%><br>

 <%= link_to activity.owner.firtname || activity.owner.username, activity.owner if activity.owner %>

followed 

<% if activity.trackable %>


<%= link_to User.find(activity.trackable.followed_id).firtname  || User.find(activity.trackable.followed_id).username, User.find(activity.trackable.followed_id)  %>  .

<% else %>
someone, whom he later unfollowed .
<% end %>