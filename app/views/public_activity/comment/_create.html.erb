<%= image_tag("icons/post.create.svg", size: "20") %>
<small><strong><%= time_ago_in_words(activity.created_at) %> ago</strong></small>
<% if activity.owner.picture? %><%= image_tag activity.owner.picture.url, :size => "30x30"  %>
       
        <%end%><br>

   
   <% if activity.trackable %>

   
       
        

    <%= link_to activity.owner.firtname || activity.owner.username, activity.owner if activity.owner %>








commented on 

<% if activity.trackable.post.user == activity.owner %>
 his own 

 <% else %>

<%= activity.trackable.post.user.username ||  activity.trackable.post.user.firtname + ' '+activity.trackable.post.user.lastName  %>'s

 <% end %>
 

  <%= link_to " post", activity.trackable.post %>

   


<% else %>
  <%= link_to activity.owner.firtname || activity.owner.username, activity.owner if activity.owner %> wrote a comment, which was then removed.
<% end %>